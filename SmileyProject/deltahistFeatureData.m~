% Import data into matlab
uiimport

%%
display(feature_import)

n_groups = 5
[group, shapes] = kmeans(feature_import, ...
    n_groups, 'Replicates', 10, 'Distance', 'cosine');

figure(1)
plot(shapes(2, 35:70)')

figure(2)
plot(feature_import( group== 5, :)')
%%
plot(feature_import')
